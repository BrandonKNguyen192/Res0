# Provisioned by: stripe projects add openrouter/api
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Provisioned by: stripe projects add auth0/client   (A wires these)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
APP_BASE_URL=http://localhost:3000
AUTH0_SECRET=

# Provisioned by: stripe projects add neon/postgres  (note the name — NOT DATABASE_URL)
NEON_POSTGRES_CONNECTION_STRING=

# Stripe Billing — separate from Stripe Projects   (B wires these)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
